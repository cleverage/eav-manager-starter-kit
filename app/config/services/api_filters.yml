services:
    api.filter.author.search:
        parent: CleverAge\EAVApiPlatformBundle\Filter\SearchFilter
        arguments:
            - { olCode: ~ }
        tags:
            - { name: api_platform.filter, id: api.filter.author.search }
