sidus_data_grid:
    configurations:
        base:
            query_handler:
                provider: sidus.eav
                sortable:
                    - id
                    - createdAt
                    - updatedAt
            columns:
                id:
                    label: '#'
                label:
                    template: '%clickable.template%'
                createdAt: ~
                updatedAt: ~
                actions:
                    template: '%eav_datagrid.actions%'
            actions:
                create: '%create.button%'
            reset_button:
                attr:
                    data-target: tg_center
